import * as React from "react";

import styled from "styled-components";

const Style = styled.span`
  font-family: PixelMplus10 Regular;
`;

const Logo = () => {
  return <Style>DLCode</Style>;
};

export default Logo;
